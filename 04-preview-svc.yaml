apiVersion: v1
kind: Service
metadata:
  name: nginx-green
spec:
  selector:
    purpose: blue-green
    project: roboshop
    color: green
  ports:
    - protocol: TCP
      port: 80 #service port
      targetPort: 80 #containeer port